enclave {
	untrusted 
	{
		int ocall_decent_la_send_trusted_msg([user_check] void* connectionPtr, [in, string] const char* senderID, [in, string] const char *msg);
	};
	
	trusted 
	{
		public sgx_status_t ecall_decent_proc_send_app_sign_req([in, string] const char* peerId, [user_check] void* connectionPtr, [in, string] const char* jsonMsg);
	};
};