enclave {
	trusted 
	{
		public sgx_status_t ecall_decent_app_process_ias_ra_report([in, string] const char* reportStr);
		
		public sgx_status_t ecall_decent_app_get_x509([in, string] const char* decentId, [user_check] void* connectionPtr);

		public size_t ecall_decent_app_get_x509_pem([out, size = buf_len] char* buf, size_t buf_len);
	};
};