enclave {
	
	untrusted {
		void ocall_print_string([in, string] const char *str);

		void ocall_log_w([in, string] const char *file, [in, string] const char *line, [in, string] const char *str);
	};

};